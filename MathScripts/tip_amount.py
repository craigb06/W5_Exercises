# Define the known values
total = 320
gratuity = .20

# Calculate the unknown
tip_amount = total * gratuity

# Display the results
print('The tip on a $' + str(total) + ' restaurant bill is $' + format(tip_amount, '.2f'))